FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["sh", "-c", "celery -A app.workers.celery_app worker --loglevel=${CELERY_LOG_LEVEL:-info}"]
